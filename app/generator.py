'''
load from db
have /sprites/ too
fetch pokemon, level data
have aliases for pokemon not yet added (gen 2 bug -> gen 1 bug)

load type from db based on name (startswith) if two (metagross psychic/earth), do random choice
have dictionary with positioning of attribute box offset, add a lil randomness

'''